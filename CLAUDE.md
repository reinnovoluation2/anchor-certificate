# CLAUDE.md — ANCHOR 수료증 발급 앱 프로젝트 규칙

이 파일은 이 프로젝트에서 AI가 반드시 지켜야 할 규칙을 적어둔 문서입니다.
작업을 시작하기 전에 이 파일과 함께 `prd_lite.md`(기획서), `PLAN.md`(작업 계획), `DESIGN.md`(설계서)를 모두 읽으십시오.

---

## 프로젝트 한 줄 요약

고려대학교 ANCHOR사업단 담당자가 엑셀로 수료자 명단을 올려두면,
학생이 학번과 이름을 입력해 자기 수료증을 국문 또는 영문 PDF로 직접 발급받는 앱입니다.

자세한 내용은 [prd_lite.md](prd_lite.md)에 있습니다. **기획서와 이 규칙서가 어긋나면 기획서를 따르고, 어긋난 사실을 알려주십시오.**

---

## 1. 언어

- 모든 설명, 주석, 커밋 메시지, 화면 문구는 **한국어**로 작성합니다.
- 변수명과 함수명은 영어를 써도 되지만, 주석은 한국어로 답니다.

## 2. 파일 위치

- 새 파일은 **이 프로젝트 폴더(`my-app3`) 안에만** 만듭니다.
- 프로젝트 밖(바탕화면, 다른 폴더, 시스템 경로)에 파일을 만들지 않습니다.
- 임시 파일이 필요하면 프로젝트 안에 만들고, 끝나면 아래 8번 규칙에 따라 정리합니다.

## 3. 기술 스택 (고정)

- **Next.js** 로 고정합니다.
- 배포는 **Vercel** 을 씁니다.
- 다른 프레임워크로 바꾸자고 제안하지 않습니다. 마이그레이션 계획도 제안하지 않습니다.
- 스택을 바꿔야 할 기술적 이유가 생기면, 직접 바꾸지 말고 **이유를 설명하고 판단을 요청**하십시오.

## 4. 변경 보고

- 코드를 바꾸면 **무엇을 왜 바꿨는지 한 줄로** 반드시 알려줍니다.
- 여러 파일을 바꿨으면 파일마다 한 줄씩 적습니다.
- 예: `app/page.tsx — 학번 입력 칸 추가 (명단 조회에 학번이 필요해서)`

## 5. 비밀 정보

- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, **절대 커밋하지 않습니다.**
- 비밀번호, 토큰, 키를 **코드에 직접 쓰지 않습니다.** 반드시 `.env`에서 읽어옵니다.
- 비밀 값을 채팅 화면에 출력하지 않습니다. 로그에도 남기지 않습니다.
- `.env.example`처럼 값이 비어 있는 예시 파일은 커밋해도 됩니다.

## 6. 외부 서비스 인증

- 외부 서비스 인증이 필요하면 **토큰 값을 사용자에게 묻지 말고, 채팅에 출력하지도 말고**, `.env`에 있는 값을 읽어서 사용합니다.
- Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업합니다.
- Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업합니다.
- `.env`에 필요한 값이 없으면, 값을 묻지 말고 **어떤 이름의 값이 필요한지만** 알려주십시오.

## 7. 배포와 되돌리기 어려운 작업

- 배포, 강제 푸시, 데이터 삭제처럼 되돌리기 어려운 작업은 **먼저 확인을 받고** 실행합니다.

## 8. 파일 삭제

- 파일을 지워야 할 때는 **바로 삭제하지 않습니다.**
- 프로젝트 안에 `trash-can` 폴더를 만들어 그 안으로 **옮겨만 둡니다.**
- 작업이 끝난 뒤 사용자가 직접 확인하고 삭제합니다.
- `trash-can` 폴더는 `.gitignore`에 등록합니다.

## 9. 서브에이전트

- 이미 설치된 서브에이전트는 필요할 때마다 **적극 활용**합니다.
- 여러 파일을 훑어야 하거나, 코드 품질·보안 점검이 필요하거나, 기획서와 구현이 맞는지 대조할 때가 특히 그렇습니다.

---

## 10. 작업 절차 (검증 루프)

작업할 때마다 아래를 **반복**합니다. 통과할 때까지 돕니다.

```
1) 변경한다
      ↓
2) 결과를 직접 확인한다  (브라우저로 열기 / 실행)
      ↓
3) 스스로 코드 리뷰한다
      ↓
4) 문제가 있으면 → 고치고 다시 1)로
   문제가 없으면 → 무엇을 왜 바꿨는지 한 줄로 요약한다
```

### 1) 변경한다
- 한 번에 한 가지만 바꿉니다. 여러 가지를 한꺼번에 바꾸면 무엇이 문제인지 알 수 없습니다.

### 2) 결과를 직접 확인한다
- **반드시 실제로 실행해서 눈으로 봅니다.** 개발 서버를 띄우고 브라우저로 해당 화면을 엽니다.
- "코드가 맞아 보인다", "논리상 될 것이다"로 이 단계를 대신하지 않습니다.
- 화면 오류뿐 아니라 **브라우저 콘솔과 터미널 로그**도 함께 봅니다.
- 이 프로젝트에서 특히 확인할 것:
  - PDF를 실제로 내려받아 열어보고, **한글이 깨지지 않는지** 봅니다.
  - **화면 미리보기와 PDF의 내용·배치가 같은지** 나란히 비교합니다.
  - 엑셀 업로드는 정상 파일뿐 아니라 **잘못된 파일도** 올려서 제대로 거부하는지 봅니다.
  - 명단에 없는 학번, 이름이 틀린 경우도 넣어봅니다.

### 3) 스스로 코드 리뷰한다
- 기획서(`prd_lite.md`)의 "AI가 꼭 지켜야 할 규칙"을 어긴 곳이 없는지 대조합니다.
- 기획서 4번(안 만들 기능)에 있는 것을 만들지 않았는지 확인합니다.
- 비밀 값이 코드에 박혔거나 화면·로그에 노출되지 않는지 확인합니다.
- 필요하면 코드 리뷰용 서브에이전트를 씁니다 (9번 규칙).

### 4) 고치고 다시 1)로
- 문제를 고쳤으면 다시 1)부터 돕니다. 고쳤다고 넘어가지 말고 **다시 확인**합니다.
- **같은 문제로 세 번 돌아도 해결되지 않으면 멈추고**, 무엇을 시도했고 무엇이 막혔는지 알려주십시오. 계속 돌리지 않습니다.

### 통과하면
- 4번 규칙대로 **무엇을 왜 바꿨는지 한 줄로** 요약해 알려줍니다.
- 확인하지 못한 부분이 있으면 숨기지 말고 함께 적습니다. 예: "PDF 한글은 확인했으나 모바일 화면은 확인하지 못했습니다."

---

## 이 프로젝트에서 특히 조심할 것

기획서에서 나온 규칙 중 개발할 때 자주 어긋나는 것들입니다.

- **PDF는 서버에서 만듭니다.** 브라우저에서 만들면 사업단장 직인 이미지가 학생 컴퓨터로 내려가 위조에 쓰일 수 있습니다.
- **PDF에 한글 폰트를 포함**시킵니다. 포함하지 않으면 한글이 깨집니다.
- **화면 미리보기와 PDF의 내용·배치가 같아야 합니다.**
- **단위과제번호와 프로그램번호는 글자(텍스트)로 다룹니다.** 숫자로 다루면 `01`이 `1`이 되어 관리번호가 틀어집니다.
- **엑셀 업로드에 잘못된 줄이 있으면 통째로 취소**합니다. 일부만 저장되면 명단이 어긋납니다.
- **기획서 4번(안 만들 기능)에 적힌 것은 만들지 않습니다.** 있으면 좋을 것 같아도 만들지 않습니다.

---

## 현재 폴더 상태

```
my-app3/
├── src/app/                    화면과 서버 통로
│   ├── page.tsx  Issue.tsx     학생 발급 (S1 조회 → S2 선택 → S3 확인)
│   ├── admin/                  담당자 로그인·명단 관리
│   └── api/                    lookup · certificate · admin
├── src/lib/                    공용 코드
│   ├── roster.ts               ★ 명단 9개 항목 정의와 검증 (유일한 정의 지점)
│   ├── certificate.ts          ★ 수료증 서식 (화면·PDF가 함께 씀)
│   ├── excel.ts  store.ts      엑셀 읽기·쓰기 / 저장소
│   ├── pdf.ts  fonts.ts  assets.ts   PDF 생성·글꼴·그림
│   └── session.ts  rate-limit.ts     로그인·출입증 / 시도 제한
├── src/components/Header.tsx   가로형 로고 머리글
├── public/fonts/               본명조·Noto Sans KR
├── public/logo/                가로형·세로형 로고
├── private/                    직인 (웹으로 못 내려받음, 커밋 금지)
├── scripts/                    확인용 스크립트 3개
├── .data/roster.json           개발용 명단 저장 (Supabase 값 넣으면 그쪽으로)
├── trash-can/                  삭제 대기 (규칙 8번)
├── .env  .env.local  .env.example    비밀 값
├── CLAUDE.md  prd_lite.md  PLAN.md  DESIGN.md
└── 합성_수료자명단_샘플_v2.xlsx   개발용 가짜 명단 31건, 9개 항목
```

**PLAN 1~20번 완료.** 학생 발급과 담당자 명단 관리가 끝에서 끝까지 동작합니다.

확인용 스크립트 (개발 서버를 띄운 뒤 실행):

```
npx tsx scripts/check-roster.mts   명단 읽기·검증 19가지 확인
npx tsx scripts/compare.mts        미리보기와 PDF가 같은지 확인
npx tsx scripts/sweep.mts          합성 명단 31건 전체 흐름
```

아직 남은 것:

- **직인 이미지** — `private/seal.png` 에 넣어야 합니다. 없으면 PDF에 점선 자리만 찍히고, 그 PDF는 정식 수료증이 아닙니다.
- **Supabase 연결** — `.env` 에 `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` 가 없어 지금은 `.data/roster.json` 파일에 저장합니다. Vercel은 파일을 쓸 수 없으므로 배포 전에 반드시 필요합니다.
- **PLAN 21번 배포** — 확인을 받고 진행합니다 (규칙 7번).
- `git init` 은 아직 하지 않았습니다.
