# DESIGN.md — ANCHOR 수료증 발급 앱 설계서

- 작성일: 2026-08-19
- 근거 문서: [PLAN.md](PLAN.md), [prd_lite.md](prd_lite.md), [CLAUDE.md](CLAUDE.md)

이 문서는 **무엇을 어떻게 만들 것인지**를 적은 설계서입니다.
개발을 모르셔도 읽을 수 있게 썼습니다. 어려운 이름이 나올 때마다 한 줄 설명을 붙였습니다.

---

# 1. 화면 구성

전체 화면은 **5개**입니다. 학생용 3개, 담당자용 2개.

## 1-1. 화면 지도

```
[학생]  누구나 들어옴 (로그인 없음)
  S1 조회 화면  →  S2 프로그램 선택  →  S3 수료증 확인 · 다운로드

[담당자]  공용 계정으로 로그인해야 들어옴
  A1 로그인 화면  →  A2 명단 관리 화면
```

## S1. 조회 화면 (첫 화면)

학생이 주소로 들어오면 처음 보는 화면입니다.

```
┌──────────────────────────────────┐
│                                  │
│        수료증 발급               │   ← 제목 (크림슨)
│        ─────                     │
│                                  │
│   ANCHOR사업 프로그램 수료증을    │   ← 안내 한 줄
│   직접 발급받으실 수 있습니다.    │
│                                  │
│   학번  [                    ]   │
│   이름  [                    ]   │
│                                  │
│        [    조회하기    ]        │   ← 크림슨 버튼
│                                  │
│   ※ 명단에 등록된 수료자만        │   ← 작은 안내
│      발급받을 수 있습니다.        │
│                                  │
└──────────────────────────────────┘
```

- 입력칸은 **학번, 이름 딱 2개**입니다.
- 조회에 실패하면 **어느 쪽이 틀렸는지 알려주지 않습니다.** (기획서 규칙)
  - 화면에 뜨는 문구: `입력하신 정보로 수료 내역을 찾을 수 없습니다. 학번과 이름을 확인해 주세요.`

## S2. 프로그램 선택 화면

조회에 성공하면 나옵니다.

```
┌──────────────────────────────────┐
│   김서준 님, 확인되었습니다.      │   ← 이름으로 인사
│   ─────                          │
│                                  │
│   수료한 프로그램을 선택하세요.   │
│                                  │
│   ○ 2026  글로벌 산학협력 인턴십  │   ← 실제 수료한 것만 나옴
│   ○ 2026  해외 단기연수 프로그램  │      (연도를 앞에 붙임)
│                                  │
│   언어                           │
│   ● 국문    ○ English            │
│            (영문명이 없는 프로그램은  │
│             English 선택 불가)       │
│                                  │
│        [  수료증 확인  ]         │
│                                  │
└──────────────────────────────────┘
```

- 목록에는 **그 학번이 실제로 수료한 프로그램만** 나옵니다. 수료하지 않은 것은 아예 안 보입니다.
- 프로그램이 하나뿐이면 그것만 나옵니다. (선택지가 하나여도 화면은 같습니다)
- **같은 프로그램이라도 연도가 다르면 각각 따로 나옵니다.** 발급 관리번호에 연도가 들어가 서로 다른 수료증이기 때문입니다.
- 프로그램 영문명이 비어 있으면 `English` 를 고를 수 없게 흐리게 표시하고, 아래 문구를 보여줍니다.
  - `이 프로그램은 영문 수료증을 준비 중입니다. 국문으로만 발급됩니다.`

## S3. 수료증 확인 · 다운로드 화면

```
┌──────────────────────────────────┐
│  [ ← 다시 선택 ]   [ PDF 받기 ]  │   ← 위쪽 버튼 줄
├──────────────────────────────────┤
│                                  │
│   ┌────────────────────────┐    │
│   │ 제 2026-01-01 호        │    │   ← A4 세로 한 장 (연도-단위과제-프로그램)
│   │                        │    │      (실제 수료증 모양 그대로)
│   │      수 료 증          │    │
│   │      ─────             │    │
│   │  학 번 : 2021320014     │    │
│   │  성 명 : 김 서 준       │    │
│   │                        │    │
│   │  위 사람은 고려대학교   │    │
│   │  ANCHOR사업단이 주최한  │    │
│   │  「글로벌 산학협력      │    │
│   │  인턴십」 과정을 성실히 │    │
│   │  이수하였으므로 이 증서 │    │
│   │  를 수여합니다.         │    │
│   │                        │    │
│   │    2026년 8월 19일      │    │
│   │                        │    │
│   │  고려대학교 ANCHOR사업단장 │    │
│   │            ┌ ─ ─ ┐     │    │   ← 직인 자리 (점선)
│   │            └ ─ ─ ┘     │    │
│   │  PDF에 직인이 찍혀     │    │
│   │  나옵니다.              │    │
│   └────────────────────────┘    │
│                                  │
└──────────────────────────────────┘
```

- **먼저 보고 나서** `PDF 받기`를 눌러야 내려받아집니다. 바로 다운로드되지 않습니다.
- **미리보기에는 직인이 없습니다.** 점선 상자로 자리만 보여줍니다. 직인 이미지가 브라우저로 내려가면 위조에 쓰일 수 있기 때문입니다.
- 화면과 PDF는 **직인 자리를 뺀 나머지**가 똑같아야 합니다. (성공 기준 6번)
- PDF에서는 점선 상자 자리에 실제 직인이 `ANCHOR사업단장` 글자 오른쪽 끝에 약 40% 겹쳐 찍힙니다. 위치와 크기는 고정입니다.

### S3 — 영문 수료증

같은 자리에 영문판이 들어갑니다. 서식(테두리·여백·줄 위치)은 국문과 같고 글자만 바뀝니다.

```
   ┌────────────────────────┐
   │ No. 2026-01-01          │
   │                        │
   │      CERTIFICATE       │
   │      OF COMPLETION     │
   │      ─────             │
   │  Student ID : 2021320014│
   │  Name : KIM SEOJUN      │
   │                        │
   │  This is to certify    │
   │  that the above-named  │
   │  student has success-  │
   │  fully completed the   │
   │  「Global Industry-    │
   │  Academia Cooperation  │
   │  Internship」 program  │
   │  organized by the      │
   │  ANCHOR Project Group, │
   │  Korea University.     │
   │                        │
   │   August 19, 2026      │
   │                        │
   │  Director,        ┌ ─ ┐│
   │  ANCHOR Project   └ ─ ┘│
   │  Group,                │
   │  Korea University      │
   └────────────────────────┘
```

- 프로그램명은 명단의 **프로그램 영문명**을 씁니다. 비어 있으면 이 화면 자체에 오지 못합니다.
- 상위기관(Seoul ANCHOR Center 등)은 넣지 않습니다. 수료증을 주는 주체는 고려대학교 사업단입니다.

### 휴대폰에서의 S3

A4 한 장은 휴대폰 화면보다 넓습니다. 가로로 밀어 보게 하지 않고 **비율을 유지한 채 화면 폭에 맞춰 줄여서** 보여줍니다.

- 수료증은 A4 비율 그대로 축소합니다 (`transform: scale`). 글자가 작아져도 배치는 같습니다.
- `PDF 받기` 버튼은 화면 아래에 고정해 항상 손이 닿게 합니다.
- 내려받은 PDF는 축소와 무관하게 **항상 A4 원본 크기**입니다.

## A1. 담당자 로그인 화면

```
┌──────────────────────────────────┐
│        담당자 로그인             │
│        ─────                     │
│   아이디    [              ]     │
│   비밀번호  [              ]     │
│        [    로그인    ]          │
│                                  │
│   ※ 사업단 공용 계정입니다.       │
└──────────────────────────────────┘
```

- 회원가입 링크, 비밀번호 찾기 링크는 **없습니다.**

## A2. 명단 관리 화면

로그인해야만 들어옵니다. 주소를 직접 쳐도 로그인 화면으로 돌아갑니다.

```
┌──────────────────────────────────────────┐
│  수료자 명단 관리          [ 로그아웃 ]   │
├──────────────────────────────────────────┤
│                                          │
│  1단계  명단 서식 받기                    │
│  ┌────────────────────────────────────┐ │
│  │ 빈 엑셀 서식을 받아 채워 주세요.    │ │
│  │        [ 엑셀 서식 내려받기 ]       │ │
│  └────────────────────────────────────┘ │
│                                          │
│  2단계  채운 명단 올리기                  │
│  ┌────────────────────────────────────┐ │
│  │  파일을 여기에 끌어다 놓거나        │ │
│  │        [ 파일 선택 ]                │ │
│  └────────────────────────────────────┘ │
│                                          │
│  결과                                    │
│  ┌────────────────────────────────────┐ │
│  │ ✅ 31건 처리 — 새로 28건, 덮어쓴 것 │ │
│  │    3건                              │ │
│  │                                    │ │
│  │ ⚠ 영문 수료증 발급 불가 1개 프로그램 │ │
│  │    07-02 데이터 분석 실무과정       │ │
│  │    (프로그램 영문명이 비어 있음)     │ │
│  └────────────────────────────────────┘ │
│                                          │
│  현재 저장된 명단 : 2026년 31건          │
│                                          │
└──────────────────────────────────────────┘
```

**업로드가 실패했을 때는 결과 칸이 이렇게 바뀝니다.**

```
  ┌────────────────────────────────────┐
  │ ❌ 저장하지 않았습니다.             │
  │                                    │
  │  · 5번째 줄 : 단위과제번호 "04"는  │
  │    없는 번호입니다                 │
  │  · 12번째 줄 : 학번이 비어 있습니다 │
  │                                    │
  │  파일을 고쳐서 다시 올려 주세요.    │
  └────────────────────────────────────┘
```

- **한 건도 저장되지 않습니다.** 절반만 들어가는 일은 없습니다.

**명단이 아직 하나도 없을 때**는 아래쪽 표시가 이렇게 바뀝니다.

```
  현재 저장된 명단 : 아직 등록된 명단이 없습니다. 1단계부터 진행해 주세요.
```

이 상태에서 학생이 조회하면 평소의 실패 문구가 나옵니다. "명단이 비었다"고 알려주지 않습니다.

---

# 2. 데이터 흐름 (입력 → 처리 → 출력)

무엇을 넣으면 → 안에서 무슨 일이 일어나고 → 무엇이 나오는지입니다.

## 흐름 ①  엑셀 서식 내려받기

```
입력    담당자가 [엑셀 서식 내려받기] 클릭
  ↓
처리    서버가 항목 9개짜리 빈 엑셀을 그 자리에서 만듦
        (첫 줄 = 항목 이름, 둘째 줄 = 예시 한 줄)
  ↓
출력    ANCHOR_수료자명단_서식.xlsx 파일이 내려받아짐
        (둘째 줄의 예시는 지우지 않고 올려도 됨 — 흐름②에서 건너뜀)
```

## 흐름 ②  명단 올리기

```
입력    담당자가 채운 엑셀 파일 선택
  ↓
처리    1. 항목 이름·순서가 서식과 같은지 본다      → 다르면 여기서 중단
        2. 둘째 줄이 서식의 예시 줄과 똑같으면 건너뛴다
        3. 한 줄씩 검사한다
             · 빈 칸 없는가 (프로그램 영문명은 비어도 통과)
             · 단위과제번호가 01/02/03/07 중 하나인가
             · 학번이 숫자 10자리인가
           → 틀린 줄이 하나라도 있으면 전부 취소
        4. 모두 통과하면 한꺼번에 저장
             같은 (연도+단위과제+프로그램+학번) 이면 덮어씀
  ↓
출력    성공 → "31건 처리 — 새로 28건, 덮어쓴 것 3건"
               (영문명이 빈 프로그램이 있으면 함께 알림)
        실패 → "저장하지 않았습니다" + 몇 번째 줄이 왜 틀렸는지
```

> **중요** — 3번 검사가 **전부 끝난 뒤에** 4번 저장이 시작됩니다.
> 검사하면서 저장하면 중간에 멈출 때 절반만 들어갑니다.

## 흐름 ③  학생 조회

```
입력    학번 + 이름
  ↓
처리    0. 최근 시도 횟수를 본다 → 10분에 10회를 넘겼으면 잠시 막는다
        1. 이름에서 공백을 모두 뺀다 ("김 서준" → "김서준")
        2. 명단에서 학번이 같고 이름도 같은 줄을 찾는다
        3. 없으면 → 실패 (어느 쪽이 틀렸는지는 안 알려줌)
        4. 있으면 → 그 학번이 수료한 프로그램 목록을 모은다
        5. 서버가 임시 출입증을 발급한다 (아래 설명)
  ↓
출력    프로그램 목록 (S2 화면)
```

> **임시 출입증이란** — 조회에 성공했다는 표시를 서버가 잠시 기억해 두는 것입니다.
> 이렇게 하는 이유는 **학번과 이름이 주소창에 남지 않게** 하기 위해서입니다.
> 주소창에 남으면 화면을 캡처하거나 링크를 공유할 때 개인정보가 함께 나갑니다.
>
> **자세히** — 서버가 서명한 쿠키 하나로 만듭니다 (`SESSION_SECRET` 사용).
> 유효 시간은 **30분**이고, 지나면 첫 화면(S1)으로 돌려보냅니다.
> 쿠키에는 확인된 학번만 담고 이름은 담지 않습니다.

## 흐름 ④  수료증 미리보기

```
입력    프로그램 선택 + 언어 선택 (국문 / English)
  ↓
처리    1. 임시 출입증이 유효한지 확인 → 없으면 첫 화면으로
        2. 명단에서 그 사람 · 그 프로그램 줄을 가져온다
        3. 발급 관리번호를 만든다
             연도 + 단위과제번호 + 프로그램번호  →  2026-02-05
        3-1. 발급일자를 정한다 — 지금 날짜 (서버 기준 한국시간)
        4. 언어에 맞는 이름과 프로그램명을 고른다
             국문 → 국문 이름 + 국문 프로그램명
             English → 영문 이름 + 프로그램 영문명
        5. 수료증 서식에 값을 끼워 넣는다
  ↓
출력    화면에 A4 한 장짜리 수료증 (S3 화면)
```

## 흐름 ⑤  PDF 내려받기

```
입력    [PDF 받기] 클릭
  ↓
처리    1. 서버가 ④와 똑같은 수료증 화면을 만든다
        2. 서버 안에서 그 화면을 열어 A4 한 장으로 찍는다
        3. 이때 직인 이미지와 한글 글꼴을 PDF 안에 넣는다
  ↓
출력    국문 → 수료증_글로벌산학협력인턴십_김서준_국문.pdf
        영문 → Certificate_GlobalIndustryAcademiaCooperationInternship_KIM_SEOJUN_EN.pdf
```

> **왜 서버에서 찍나요?** — 학생 컴퓨터에서 만들면 **직인 이미지가 학생 컴퓨터로 내려갑니다.**
> 그 이미지를 저장해 다른 문서에 붙이면 가짜 수료증이 됩니다.
> 서버에서 찍으면 학생은 완성된 PDF만 받고, 직인 이미지 자체는 손에 넣을 수 없습니다.

## 저장하는 내용 (명단 한 줄의 모습)

**아래 "항목" 칸의 글자가 엑셀 첫 줄에 들어가는 이름 그대로입니다.** 한 글자라도 다르면 업로드가 거부되므로 코드와 서식이 이 표를 따라야 합니다.

| 항목 | 예시 | 비고 |
|---|---|---|
| 해당연도 | `2026` | 글자로 다룸 |
| 단위과제번호 | `02` | **글자로 다룸** — 숫자로 하면 `2`가 되어 관리번호가 틀어짐 |
| 프로그램번호 | `05` | 글자로 다룸 |
| 프로그램명 | `글로벌 인재 취업연계 프로그램` | 국문 수료증에 씀 |
| 프로그램 영문명 | `Global Talent Employment Program` | **유일하게 비워둘 수 있음.** 비면 영문 수료증 발급 불가 |
| 학번 | `2022740033` | 글자로 다룸. 숫자 10자리 |
| 국문 이름 | `리 웨이` | 조회에 씀 |
| 영문 이름 | `LI WEI` | 영문 수료증에 씀 |
| 고려대학교 이메일 | `li.wei@korea.ac.kr` | 이번엔 저장만 |

**같은 사람으로 보는 기준** = 해당연도 + 단위과제번호 + 프로그램번호 + 학번
이 네 개가 같으면 같은 줄로 보고 덮어씁니다.

---

# 3. 기술 선택

## 3-1. 기본

| 기술 | 한 줄 설명 |
|---|---|
| **Next.js** | 화면과 서버를 한 덩어리로 만드는 도구입니다. Vercel에 그대로 올릴 수 있어 기본으로 씁니다. |
| **Vercel** | 만든 앱을 인터넷에 올려 주소를 만들어 주는 곳입니다. |

## 3-2. 추가로 필요한 것

Next.js만으로는 안 되는 일이 있어서 아래를 더 씁니다. 각각 **왜 필요한지**를 함께 적었습니다.

| 기술 | 한 줄 설명 | 왜 필요한가 |
|---|---|---|
| **Supabase** | 인터넷에 있는 데이터 창고입니다. 엑셀 대신 명단을 여기에 보관합니다. | Vercel은 서버에 파일을 저장할 수 없습니다. 명단을 둘 곳이 따로 필요합니다. |
| **ExcelJS** | 코드가 엑셀 파일을 읽고 쓸 수 있게 해 주는 도구입니다. | 서식을 만들어 주고, 올라온 엑셀을 읽어야 합니다. |
| **Zod** | 들어온 값이 규칙에 맞는지 검사해 주는 도구입니다. | "단위과제번호는 01/02/03/07만" 같은 규칙을 한 곳에 모아 둡니다. |
| **Puppeteer** (+ @sparticuz/chromium) | 서버 안에서 크롬 브라우저를 열어, 화면을 그대로 PDF로 찍어 주는 도구입니다. | 화면과 PDF가 똑같아야 한다는 조건을 지키는 가장 확실한 방법입니다. |
| **본명조** (Noto Serif KR, 글꼴 파일) | 공식 문서에 쓰는 한글 명조 글꼴입니다. PDF 안에 넣어 둡니다. | 넣지 않으면 PDF에서 한글이 네모(□□□)로 깨집니다. 자세한 내용은 3-4 참고. |
| **Tailwind CSS** | 화면 꾸미기를 짧게 쓸 수 있게 해 주는 도구입니다. | Next.js를 만들 때 같이 들어옵니다. 안 써도 되지만 있으면 편합니다. |

## 3-3. PDF 만드는 방법 — 두 가지 중 선택

여기가 이 프로젝트에서 **가장 중요한 기술 결정**입니다.

| | 방법 A: 크롬으로 찍기 (Puppeteer) | 방법 B: PDF를 직접 그리기 |
|---|---|---|
| 어떻게 | 화면을 만든 뒤 서버 안의 크롬으로 사진 찍듯 PDF로 만듭니다 | 글자와 선의 위치를 하나하나 좌표로 지정해 PDF를 그립니다 |
| 화면과 PDF 일치 | **똑같습니다** (같은 화면을 찍으니까) | 화면과 따로 만들어서 **어긋나기 쉽습니다** |
| 서식 고치기 | 화면만 고치면 PDF도 같이 바뀝니다 | 두 군데를 각각 고쳐야 합니다 |
| 무게 | 무겁습니다 (크롬이 통째로 들어감) | 가볍습니다 |
| Vercel 위험 | 용량 제한에 걸릴 수 있습니다 | 문제없습니다 |

**추천: 방법 A.**
기획서에 "화면 미리보기와 PDF의 내용·배치가 완전히 같아야 한다"가 못 박혀 있는데, 방법 B는 이 조건을 지키려면 같은 서식을 두 번 만들어야 하고 시간이 지날수록 반드시 어긋납니다.

**단, 위험이 하나 있습니다.** Vercel에는 올릴 수 있는 용량 한도가 있는데 크롬이 무거워 아슬아슬합니다.
그래서 [PLAN.md](PLAN.md) **2번 작업에서 이것부터 확인**하도록 순서를 잡아 놓았습니다.
거기서 막히면 그때 방법 B로 바꿔도 늦지 않습니다. 다른 작업을 하기 전이라 버릴 게 없습니다.

## 3-3-1. 색

| 쓰임 | 색 | 값 |
|---|---|---|
| 주색 (제목·버튼·테두리·구분선) | 고려대 크림슨 | `#862633` |
| 본문 글자 | 검정에 가까운 회색 | `#1A1A1A` |
| 배경 | 흰색 | `#FFFFFF` |
| 보조 안내 글자 | 회색 | `#555555` |

크림슨은 **제목·버튼·테두리·구분선에만** 씁니다. 넓은 면적을 칠하거나 본문 글자에 쓰지 않습니다. 진한 붉은색이라 읽기 나빠지고 격식도 떨어집니다.

## 3-4. 글꼴

글꼴은 **웹 화면**과 **수료증** 두 곳에서 다르게 씁니다.

### 웹 화면 — 맑은 고딕

조회 화면, 로그인 화면, 명단 관리 화면에 쓰는 글꼴입니다.

```css
font-family: "맑은 고딕", "Malgun Gothic", "Noto Sans KR", sans-serif;
```

앞에서부터 찾아 있는 것을 씁니다.

| 순서 | 글꼴 | 언제 쓰이나 |
|---|---|---|
| 1 | 맑은 고딕 | 윈도우 컴퓨터 (대부분의 담당자·학생) |
| 2 | Noto Sans KR | 맥, 아이폰, 안드로이드 — 맑은 고딕이 없는 기기 |
| 3 | sans-serif | 그 외 |

> **왜 대체 글꼴이 필요한가요?** — 맑은 고딕은 **윈도우에만 들어 있는 글꼴**입니다.
> 맥이나 휴대폰에는 없어서, 지정만 해두면 그 기기에서는 제멋대로인 글꼴로 보입니다.
> 그래서 생김새가 가장 비슷한 Noto Sans KR을 뒤에 놓아 자동으로 대신 쓰이게 합니다.
>
> 맑은 고딕 파일을 서버에 올려 배포하는 것은 **라이선스 위반**입니다.
> 기기에 이미 설치된 것을 가져다 쓰는 것(위 방식)은 문제없습니다.

### 수료증 — 본명조 (Noto Serif KR)

**추천: 본명조.** 어도비와 구글이 함께 만든 무료 명조체입니다.

| | |
|---|---|
| 왜 명조체인가 | 공공기관 증서·상장은 명조체가 관례입니다. 고딕체는 안내문 느낌이 납니다 |
| 왜 본명조인가 | ① 무료이고 **PDF에 넣어 배포해도 되는 라이선스**입니다 ② 국문과 짝이 맞는 영문 글꼴(Noto Serif)이 같이 있어 **국문·영문 수료증의 인상이 같아집니다** ③ 굵기가 7단계라 제목과 본문을 자연스럽게 나눌 수 있습니다 |

```css
/* 수료증 안에서만 */
font-family: "Noto Serif KR", "본명조", serif;
```

**대안 두 가지** (본명조가 마음에 안 드시면)

| 글꼴 | 특징 | 라이선스 |
|---|---|---|
| KoPub 바탕체 | 한국출판인회의가 만든 출판용 명조. 공공기관 문서에서 자주 보입니다 | 무료, 임베딩 가능 |
| 나눔명조 | 네이버가 만든 명조. 가볍고 무난합니다 | 무료, 임베딩 가능 |

### 쓸 수 없는 글꼴

| 글꼴 | 왜 안 되나 |
|---|---|
| **휴먼명조** | 한글과컴퓨터(한컴 오피스)에 딸려 오는 글꼴입니다. 웹 서비스에 올리거나 **PDF 안에 넣어 배포할 권리가 없습니다.** 공식 증서를 라이선스 위반 글꼴로 만들면 나중에 문제가 됩니다 |
| **맑은 고딕 (수료증용)** | 윈도우 전용이라 PDF에 넣어 배포할 수 없습니다. 웹 화면에서 "설치된 것 가져다 쓰기"만 됩니다 |
| **바탕체 / 궁서체** | 윈도우 전용입니다. 위와 같습니다 |

### 반드시 지킬 것

- 수료증 글꼴 파일(`.ttf`)은 **프로젝트 안에 넣어 둡니다.** 인터넷에서 불러오면 서버에서 PDF를 만들 때 글꼴이 빠져 한글이 깨집니다.
- 화면 미리보기와 PDF가 **같은 글꼴 파일**을 써야 합니다. 다르면 줄 길이가 달라져 배치가 어긋납니다. (성공 기준 6번)

## 3-5. 로그인은 어떻게 하나

**추가 도구를 쓰지 않습니다.**

로그인 도구(NextAuth 등)는 회원가입, 비밀번호 찾기, 소셜 로그인 같은 것을 위한 것인데 이 프로젝트에는 **공용 계정 하나뿐**이라 전부 필요 없습니다.

```
담당자가 아이디·비밀번호 입력
  ↓
.env 파일에 적힌 값과 맞는지 서버가 비교
  ↓
맞으면 브라우저에 "로그인했음" 표시를 하나 심어 둠 (쿠키)
  ↓
담당자 화면에 들어올 때마다 그 표시를 확인
```

- 아이디와 비밀번호는 **코드에 쓰지 않고 `.env`에서 읽습니다.** (CLAUDE.md 5번)
- `.env`에 넣을 값: `ADMIN_ID`, `ADMIN_PASSWORD`

## 3-6. `.env`에 들어갈 값 목록

지금 시점에 필요한 것들입니다. **값은 직접 넣으시면 됩니다. 저에게 알려주지 않으셔도 됩니다.**

```
ADMIN_ID                    담당자 공용 아이디
ADMIN_PASSWORD              담당자 공용 비밀번호
SESSION_SECRET              로그인 표시를 위조하지 못하게 막는 임의의 긴 문자열
SUPABASE_URL                데이터 창고 주소
SUPABASE_SERVICE_ROLE_KEY   데이터 창고 열쇠
SUPABASE_ACCESS_TOKEN       Supabase 작업용 (CLAUDE.md 6번)
VERCEL_TOKEN                배포 작업용 (CLAUDE.md 6번)
```

---

# 4. 안전 장치

| 지킬 것 | 어떻게 |
|---|---|
| 남의 수료증을 못 받게 | 학번과 이름이 **둘 다** 맞아야 조회됩니다 |
| 명단을 훑어가는 것 방지 | 같은 곳에서 **10분에 10회**를 넘겨 조회하면 잠시 막습니다 |
| 개인정보가 주소창에 안 남게 | 학번·이름을 주소에 넣지 않고 서버가 잠시 기억합니다 |
| 직인 위조 방지 | 직인 이미지를 공개 폴더에 두지 않고, 서버 안에서만 읽어 PDF에 넣습니다 |
| 담당자 화면 보호 | 로그인 표시가 없으면 주소를 직접 쳐도 로그인 화면으로 돌립니다 |
| 명단이 반쯤 깨지는 것 방지 | 검사를 전부 끝낸 뒤에 저장을 시작합니다 |
| 비밀 값 유출 방지 | `.env`에서만 읽고, 화면·기록에 찍지 않습니다 |

---

# 4-1. 실패했을 때 보이는 화면

지금까지 정한 오류 화면은 엑셀 검증 실패 하나뿐이었습니다. 나머지도 정해 둡니다.

**사람이 잘못한 것**은 원인을 알려주지 않고, **시스템이 고장 난 것**은 알려줍니다. 전자는 알려주면 남의 정보를 캐는 단서가 되지만, 후자는 알려줘야 담당자가 조치할 수 있기 때문입니다.

| 상황 | 누가 보나 | 화면 문구 |
|---|---|---|
| 조회 실패 (학번·이름 불일치) | 학생 | `입력하신 정보로 수료 내역을 찾을 수 없습니다. 학번과 이름을 확인해 주세요.` |
| 조회 시도 초과 | 학생 | `조회 시도가 많습니다. 10분 뒤에 다시 시도해 주세요.` |
| 임시 표시 만료 (30분 초과) | 학생 | `시간이 지나 처음부터 다시 확인이 필요합니다.` → S1으로 이동 |
| PDF 만들기 실패 | 학생 | `수료증을 만들지 못했습니다. 잠시 뒤 다시 시도해 주세요. 계속되면 사업단으로 문의해 주세요.` |
| 저장소 연결 실패 | 학생·담당자 | `지금은 서비스를 이용할 수 없습니다. 잠시 뒤 다시 시도해 주세요.` |
| 엑셀이 아닌 파일 업로드 | 담당자 | `.xlsx 파일만 올릴 수 있습니다. 서식을 내려받아 사용해 주세요.` |
| 파일이 너무 큼 | 담당자 | `파일이 너무 큽니다. 10MB 이하로 나눠서 올려 주세요.` |
| 로그인 실패 | 담당자 | `아이디 또는 비밀번호가 맞지 않습니다.` |

---

# 5. 확인 방법

각 흐름이 제대로 되는지 보는 방법입니다. 합성 명단 31건을 씁니다.

| 무엇을 | 어떻게 확인 |
|---|---|
| 흐름 ① 서식 | 받은 엑셀을 열어 **항목 9개**와 예시 줄이 있는지 본다 |
| 흐름 ② 정상 | 합성 명단을 올려 "31건" 이 나오는지 본다 |
| 흐름 ② 실패 | 단위과제번호를 `04`로 고쳐 올려 **한 건도 저장 안 되는지** 본다 |
| 흐름 ③ 성공 | `2021320014 / 김서준` → 프로그램이 **2개** 나오는지 본다 |
| 흐름 ③ 실패 | `2021320014 / 홍길동` → 실패 문구만 나오는지 본다 |
| 흐름 ③ 외국인 | `2023740015 / 응우옌 반 안` 이 조회되는지 본다 |
| 흐름 ④ 언어 | 국문·영문을 바꿔 이름이 각각 `김서준` / `KIM SEOJUN` 인지 본다 |
| 흐름 ⑤ PDF | 받은 PDF를 열어 **한글이 안 깨지는지**, 직인과 관리번호가 있는지 본다 |
| 흐름 ⑤ 일치 | 화면과 PDF를 나란히 놓고 배치가 같은지 본다 |
| 직인 보호 | 직인 이미지 주소로 직접 들어가 **안 열리는지** 본다 |
| 미리보기 직인 | 미리보기 화면에 직인이 **없고** 점선 자리만 있는지 본다 |
| 영문 차단 | `2022330121 / 안유찬` (데이터 분석 실무과정) → English가 **안 눌리는지** 본다 |
| 예시 줄 | 서식의 예시 줄을 지우지 않고 올려도 31건만 저장되는지 본다 |
| 시도 제한 | 조회를 11번 연속 실패시켜 막히는지 본다 |
| 임시 표시 만료 | 조회 후 30분 넘게 두었다가 PDF를 받아보고 첫 화면으로 가는지 본다 |
| 빈 명단 | 명단이 0건일 때 담당자 화면 문구가 바뀌는지 본다 |
| 휴대폰 | 휴대폰 폭에서 수료증이 잘리지 않고 줄어드는지 본다 |
