# PLAN.md — ANCHOR 수료증 발급 앱 (1차 사이클)

- 작성일: 2026-08-19 (2026-08-19 문서 검토 반영)
- 근거 문서: [prd_lite.md](prd_lite.md), [DESIGN.md](DESIGN.md), [CLAUDE.md](CLAUDE.md)

---

## 이번 사이클 목표

기획서에 정의된 두 기능(담당자 명단 관리 / 학생 수료증 발급)을 **끝에서 끝까지 동작하는 하나의 앱**으로 만들어 Vercel에 올린다.
합성 명단으로 전체 흐름이 막힘 없이 도는 상태까지가 이번 사이클이다.

## 성공 기준

아래 아홉 가지가 모두 되면 이번 사이클은 끝난 것으로 본다.

1. 담당자가 공용 계정으로 로그인해야만 명단 화면에 들어갈 수 있다. 로그인 없이 주소를 직접 쳐도 막힌다.
2. 빈 엑셀 서식을 내려받아 채운 뒤 올리면 명단이 저장되고, 몇 건 추가·몇 건 덮어썼는지 화면에 나온다.
3. 항목이 틀리거나 단위과제번호가 목록에 없는 엑셀은 **한 건도 저장되지 않고** 거부되며, 몇 번째 줄이 왜 틀렸는지 나온다.
4. 학생이 학번과 이름을 맞게 넣으면 **자기가 수료한 프로그램만** 목록에 뜬다. 하나라도 틀리면 발급되지 않는다.
5. 국문과 영문 수료증이 화면에 A4 세로 한 장으로 뜨고, 확인 후 PDF로 받아진다.
6. 받은 PDF에서 **한글이 깨지지 않고**, 직인과 발급 관리번호가 제자리에 있다. 화면 미리보기와 PDF는 **직인 자리를 빼고** 내용·배치가 같다.
7. 직인 이미지 파일은 브라우저로 내려가지 않는다. 미리보기에는 자리만 표시되고, 직인은 PDF 안에만 있다.
8. 합성 명단 31건 전체로 위 흐름이 돌고, Vercel 주소로 접속해서 같은 결과가 나온다.
9. 프로그램 영문명이 빈 프로그램(합성 명단의 「데이터 분석 실무과정」)은 **English를 고를 수 없고**, 국문은 정상 발급된다.

## 전제

- 저장소는 **Supabase**로 가정한다. Vercel은 서버에 파일을 쓸 수 없어서 명단을 파일로 저장할 수 없고, CLAUDE.md 6번에 Supabase 예시가 이미 있기 때문이다. 다른 것을 쓰실 거면 4번 작업 전에 알려주시면 된다.
- 실제 수료자 정보는 쓰지 않는다. 합성 명단으로만 개발한다.
- 문서 검토에서 정한 값: 발급일자 = PDF를 만든 날(서버 한국시간) / 학번 = 숫자 10자리 / 임시 출입증 유효시간 = 30분 / 조회 시도 제한 = 같은 IP 10분에 10회.
- **개인정보 보관 기간은 아직 미정이다.** 사업단 규정을 확인해 정해야 한다. 실제 데이터를 넣기 전까지는 막히지 않는다.

---

## 작업 순서

먼저 되면 뒤가 쉬워지는 것부터 놓았다. 각 작업이 끝날 때마다 CLAUDE.md 10번 검증 루프를 돈다.

1. Next.js 프로젝트를 만든다 (App Router, TypeScript). — 이게 있어야 나머지를 얹는다
2. 본명조(Noto Serif KR) 글꼴 파일을 프로젝트에 넣고, **그 글꼴로** 한글 PDF 한 장을 서버에서 뽑아 열어본다. — 기술 위험이 가장 큰 곳이라 데이터 없이 먼저 확인한다
3. 명단 **9개 항목**의 데이터 구조와 검증 규칙을 한 곳에 정의한다 (프로그램 영문명만 비워둘 수 있음). — 업로드와 조회가 같은 정의를 쓴다
4. Supabase에 명단 테이블을 만들고 연결한다. — 저장할 곳이 있어야 업로드가 의미를 갖는다
5. 합성 명단 31건을 넣어둔다. — 화면이 없어도 조회 로직을 시험할 수 있게 된다
6. 엑셀 파일을 읽어 검증하는 함수를 만든다 (서식의 예시 줄은 건너뜀). — 업로드 화면보다 먼저 확실히 해둔다
7. 빈 엑셀 서식을 내려받는 기능을 만든다. — 3번 정의가 눈에 보이는 산출물로 굳는다
8. 담당자 공용 계정 로그인과 로그아웃을 만든다 (.env에서 읽음). — 이후 담당자 화면을 여기 뒤에 둔다
9. 명단 업로드 화면을 만든다. 잘못된 줄이 있으면 통째로 취소하고, 저장 건수와 영문 발급 불가 프로그램을 함께 보여준다. — 기능 1 완성
10. 학번과 이름으로 조회해 수료한 프로그램 목록을 보여준다. 조회 성공 시 임시 출입증을 발급하고, 조회 시도 횟수를 제한한다. — 기능 2의 입구
11. 웹 화면의 글꼴과 색을 적용한다 (맑은 고딕 → Noto Sans KR, 크림슨 #862633). — 이후 만드는 화면이 처음부터 같은 모습이 된다
12. 국문 수료증 서식을 화면에 A4 세로로 그린다. — 서식이 정해져야 영문·PDF가 따라온다
13. 영문 수료증 서식을 만든다. 프로그램 영문명이 비면 English를 고를 수 없게 막는다. — 12번 틀을 재사용
14. 발급 관리번호와 발급일자를 만들어 수료증에 찍는다. — 서식이 있어야 자리를 잡는다
15. 직인을 PDF에만 넣고, 미리보기에는 자리만 표시한다. 직인 이미지에 직접 접근하지 못하게 막는다. — 2번에서 확인한 PDF 생성 방식 위에 얹는다
16. 화면에서 확인한 뒤에만 PDF를 내려받게 연결한다. — 기능 2 완성
17. 미리보기와 PDF를 나란히 놓고 직인 자리를 뺀 배치가 같은지 맞춘다. — 어긋나면 여기서 잡는다
18. 실패했을 때 보이는 화면을 만든다 (저장소 연결 실패, PDF 생성 실패, 엑셀이 아닌 파일, 명단 0건). — 여기까지 해야 실제로 쓸 수 있다
19. 합성 명단 31건으로 전체 흐름을 훑는다 (중복 수료자, 외국인 이름, 없는 학번, 영문명이 빈 프로그램 포함). — 성공 기준 8·9번 확인
20. 휴대폰 화면에서 조회와 다운로드가 되는지 본다 (A4 수료증이 화면 폭에 맞게 줄어드는지 포함). — 기획서 5번 요구사항
21. Vercel에 배포하고 실제 주소에서 다시 확인한다. — 배포 전 확인을 받는다 (CLAUDE.md 7번)

---

## 이번 사이클에서 하지 않는 것

기획서 4번에 적힌 것들이다. 있으면 좋아 보여도 만들지 않는다.

- 담당자 회원가입·계정 여러 개·비밀번호 찾기
- 웹에서 명단을 직접 고치는 화면
- 이메일 인증 코드 발송
- 발급 관리번호 진위확인 페이지
- 발급 관리번호에 개인 순번 붙이기
- 사사문구, 소속 학과, 교육기간, 이수시간, 사업단장 성명 표기

## 미리 짚어둘 위험

| 위험 | 언제 드러나나 | 대비 |
|---|---|---|
| PDF 한글 깨짐 | 2번 | 데이터 없이 먼저 확인한다. 여기서 막히면 뒤가 다 막힌다 |
| Vercel 용량 한도에 크롬이 안 들어감 | 2번 | 여기서 막히면 PDF를 직접 그리는 방식으로 바꾼다 (DESIGN 3-3) |
| 단위과제번호 `01`이 `1`로 바뀜 | 6번 | 엑셀에서 읽을 때부터 글자로 다룬다 |
| 외국인 학생 이름 대조 실패 | 19번 | 합성 명단에 외국인 7명을 넣어뒀다. **안 되면 기획서 조회 규칙을 바꿔야 하므로 멈추고 판단을 요청한다.** 임의로 영문 이름 조회를 열지 않는다 |
| 미리보기와 PDF 불일치 | 17번 | 같은 서식 코드를 화면과 PDF가 함께 쓰도록 12번에서 구조를 잡는다 |
